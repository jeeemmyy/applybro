"""Layer 4 — application outcomes from Gmail (read-only): classify replies,
never send/modify/delete, never guess (uncertain = "needs review"); turn the
log into honest insights. Gmail/Sheet clients are passed IN — this package
still imports no Google libraries itself."""
