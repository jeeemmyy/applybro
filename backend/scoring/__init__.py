"""Layer 2 — score fit: % of a job's stated requirements genuinely present
in the user's real profile (never a callback prediction), plus a transparent
salary estimate. Pure logic: no Google APIs, no app server."""
